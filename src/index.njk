{% extends '_templates/base.njk' %}

{% block page_title %}Welcome to ATX{% endblock %}

{% block page_description %}The index search description for this page goes here.{% endblock %}

{% block content %}
<div class="container">
  <div class="intro-text">
    <h2 class="heading">It's a City Like No Other</h2>
    <p>Explore Austin's unparalleled energy using this compiled guide of all the sights, bites and delights this city has to offer. From the iconic landmarks to its vibrant culinary scene, Austin has cemented its position as a hub for art and culture. Scroll below and start your journey here. Where will the ATX take you?</p>
  </div>
  <div class="row">
    <h2 class="heading">Landmarks</h2>
    {% for landmarks in landmarks.landmarks %}
    <div class="col-sm-12 col-md-6 col-lg-4">
      <div class="card">
        <img src="img/landmarks/{{ landmarks.slug }}.jpg" class="card-img-top" alt="{{ landmarks.name }}">
        <div class="card-body">
          <h4 class="card-title"><a href="landmark/{{ landmarks.slug }}.html" target= "_blank">{{ landmarks.name }}</a></h4>
          <p class="card-text"> {{landmarks.location}}</p>
          <h6> Activities: {{landmarks.activities}}</h6>
          <a href="landmark/{{ landmarks.slug }}.html" target= "_blank" class="btn btn-primary">Learn More</a>
        </div>
      </div>
    </div>
    {% endfor %}   
  </div>
</div>
 

{% endblock %}